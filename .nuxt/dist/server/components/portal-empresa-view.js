exports.ids = [53,34,39,50];
exports.modules = {

/***/ 125:
/***/ (function(module, exports) {

// Exports
module.exports = {

};


/***/ }),

/***/ 150:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _node_modules_css_loader_dist_cjs_js_ref_7_oneOf_1_0_node_modules_vue_loader_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_src_index_js_ref_7_oneOf_1_1_node_modules_sass_loader_dist_cjs_js_ref_7_oneOf_1_2_node_modules_sass_resources_loader_lib_loader_js_ref_7_oneOf_1_3_node_modules_nuxt_components_dist_loader_js_ref_0_0_node_modules_vue_loader_lib_index_js_vue_loader_options_PortalEmpresaView_vue_vue_type_style_index_0_id_7141faa9_prod_lang_scss_scoped_true___WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(125);
/* harmony import */ var _node_modules_css_loader_dist_cjs_js_ref_7_oneOf_1_0_node_modules_vue_loader_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_src_index_js_ref_7_oneOf_1_1_node_modules_sass_loader_dist_cjs_js_ref_7_oneOf_1_2_node_modules_sass_resources_loader_lib_loader_js_ref_7_oneOf_1_3_node_modules_nuxt_components_dist_loader_js_ref_0_0_node_modules_vue_loader_lib_index_js_vue_loader_options_PortalEmpresaView_vue_vue_type_style_index_0_id_7141faa9_prod_lang_scss_scoped_true___WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_cjs_js_ref_7_oneOf_1_0_node_modules_vue_loader_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_src_index_js_ref_7_oneOf_1_1_node_modules_sass_loader_dist_cjs_js_ref_7_oneOf_1_2_node_modules_sass_resources_loader_lib_loader_js_ref_7_oneOf_1_3_node_modules_nuxt_components_dist_loader_js_ref_0_0_node_modules_vue_loader_lib_index_js_vue_loader_options_PortalEmpresaView_vue_vue_type_style_index_0_id_7141faa9_prod_lang_scss_scoped_true___WEBPACK_IMPORTED_MODULE_0__);
/* harmony reexport (unknown) */ for(var __WEBPACK_IMPORT_KEY__ in _node_modules_css_loader_dist_cjs_js_ref_7_oneOf_1_0_node_modules_vue_loader_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_src_index_js_ref_7_oneOf_1_1_node_modules_sass_loader_dist_cjs_js_ref_7_oneOf_1_2_node_modules_sass_resources_loader_lib_loader_js_ref_7_oneOf_1_3_node_modules_nuxt_components_dist_loader_js_ref_0_0_node_modules_vue_loader_lib_index_js_vue_loader_options_PortalEmpresaView_vue_vue_type_style_index_0_id_7141faa9_prod_lang_scss_scoped_true___WEBPACK_IMPORTED_MODULE_0__) if(["default"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) (function(key) { __webpack_require__.d(__webpack_exports__, key, function() { return _node_modules_css_loader_dist_cjs_js_ref_7_oneOf_1_0_node_modules_vue_loader_lib_loaders_stylePostLoader_js_node_modules_postcss_loader_src_index_js_ref_7_oneOf_1_1_node_modules_sass_loader_dist_cjs_js_ref_7_oneOf_1_2_node_modules_sass_resources_loader_lib_loader_js_ref_7_oneOf_1_3_node_modules_nuxt_components_dist_loader_js_ref_0_0_node_modules_vue_loader_lib_index_js_vue_loader_options_PortalEmpresaView_vue_vue_type_style_index_0_id_7141faa9_prod_lang_scss_scoped_true___WEBPACK_IMPORTED_MODULE_0__[key]; }) }(__WEBPACK_IMPORT_KEY__));


/***/ }),

/***/ 158:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/PortalEmpresaView.vue?vue&type=template&id=7141faa9&scoped=true&
var render = function render() {
  var _vm = this,
      _c = _vm._self._c;

  return _c('section', {
    staticClass: "portal-empresa"
  }, [_vm._ssrNode("<div class=\"portal-empresa__inner\" data-v-7141faa9>", "</div>", [_vm._ssrNode("<div class=\"container\" data-v-7141faa9>", "</div>", [_vm._ssrNode("<header class=\"portal-empresa__intro\" data-v-7141faa9><h1 class=\"portal-empresa__title\" data-v-7141faa9>Portal empresarial</h1> <p class=\"portal-empresa__lead\" data-v-7141faa9>\n                    Administración de pólizas para <strong data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.empresa.nombre || 'tu empresa')) + "</strong>.\n                </p></header> <div class=\"portal-empresa__card\" data-v-7141faa9><h2 class=\"portal-empresa__card-title\" data-v-7141faa9>Datos de la empresa</h2> <div class=\"portal-empresa__company-grid\" data-v-7141faa9><div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>Nombre de la empresa</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.empresa.nombre || '—')) + "</p></div> <div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>RFC</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.empresa.rfc || '—')) + "</p></div> <div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>Correo de contacto</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.emailContactoMostrado)) + "</p></div> <div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>Teléfono</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.telefonoContactoMostrado)) + "</p></div></div> " + (_vm.imgEmpresaLogoUrl ? "<div class=\"portal-empresa__company-logo\" data-v-7141faa9><img" + _vm._ssrAttr("src", _vm.imgEmpresaLogoUrl) + " alt=\"Logotipo empresa\" class=\"portal-empresa__company-logo-img\" data-v-7141faa9></div>" : "<!---->") + "</div> " + (_vm.imgEmpresaRegistro ? "<p class=\"portal-empresa__img-empresa-notice\" data-v-7141faa9><i class=\"fas fa-info-circle\" data-v-7141faa9></i> " + (_vm.textoRamosImgEmpresa ? "\n                    Ramos mostrados según <strong data-v-7141faa9>ImgEmpresa</strong>" + _vm._ssrEscape(": " + _vm._s(_vm.textoRamosImgEmpresa) + ".\n                ") : "\n                    Datos de pólizas tomados del documento <strong data-v-7141faa9>ImgEmpresa</strong>\n                    (mapas <code data-v-7141faa9>polizaGM</code>, <code data-v-7141faa9>polizaVida</code>, <code data-v-7141faa9>polizaFuneraria</code>,\n                    <code data-v-7141faa9>polizaAccidentes</code>, etc., cuando tienen información).\n                ") + "</p>" : _vm.sinRegistroImgEmpresa && _vm.perfilImgEmpresaResuelto ? "<p class=\"portal-empresa__img-empresa-notice portal-empresa__img-empresa-notice--muted\" data-v-7141faa9><i class=\"fas fa-info-circle\" data-v-7141faa9></i>\n                No hay documento en <strong data-v-7141faa9>ImgEmpresa</strong> con\n                <code data-v-7141faa9>nombreEmpresa</code>" + _vm._ssrEscape(" igual a «" + _vm._s(_vm.empresa.nombre) + "»; se muestran todas las pólizas asociadas al RFC.\n            ") + "</p>" : "<!---->") + " "), _vm._ssrNode("<div class=\"portal-empresa__card\" data-v-7141faa9>", "</div>", [_vm._ssrNode("<h2 class=\"portal-empresa__card-title\" data-v-7141faa9>Pólizas registradas</h2> "), _vm.cargandoPolizas ? _vm._ssrNode("<div class=\"portal-empresa__loading\" data-v-7141faa9>", "</div>", [_vm._ssrNode("<i class=\"fas fa-spinner fa-spin\" data-v-7141faa9></i> <span data-v-7141faa9>Cargando pólizas…</span>")], 2) : _vm.errorPolizas ? _vm._ssrNode("<div class=\"portal-empresa__error\" data-v-7141faa9>" + _vm._ssrEscape("\n                    " + _vm._s(_vm.errorPolizas) + "\n                    ") + "<button type=\"button\" class=\"portal-empresa__btn-retry\" data-v-7141faa9>\n                        Reintentar\n                    </button></div>") : [_vm._ssrNode((_vm.tabsDisponibles.length ? "<div class=\"portal-empresa__tabs\" data-v-7141faa9>" + _vm._ssrList(_vm.tabsDisponibles, function (tab) {
    return "<button type=\"button\"" + _vm._ssrClass("portal-empresa__tab", {
      'portal-empresa__tab--active': _vm.ramoActivo === tab.key
    }) + " data-v-7141faa9>" + _vm._ssrEscape("\n                            " + _vm._s(tab.label) + "\n                        ") + "</button>";
  }) + "</div>" : "<!---->") + " "), _vm.polizaActiva ? _vm._ssrNode("<div class=\"portal-empresa__poliza-detail\" data-v-7141faa9>", "</div>", [_vm.listaPolizaActiva.length > 1 ? _vm._ssrNode("<div class=\"portal-empresa__poliza-picker\" data-v-7141faa9>", "</div>", [_vm._ssrNode("<label class=\"portal-empresa__label\" data-v-7141faa9>Seleccionar póliza</label> "), _c('select', {
    directives: [{
      name: "model",
      rawName: "v-model.number",
      value: _vm.indicePoliza,
      expression: "indicePoliza",
      modifiers: {
        "number": true
      }
    }],
    staticClass: "portal-empresa__select",
    on: {
      "change": function ($event) {
        var $$selectedVal = Array.prototype.filter.call($event.target.options, function (o) {
          return o.selected;
        }).map(function (o) {
          var val = "_value" in o ? o._value : o.value;
          return _vm._n(val);
        });
        _vm.indicePoliza = $event.target.multiple ? $$selectedVal : $$selectedVal[0];
      }
    }
  }, _vm._l(_vm.listaPolizaActiva, function (pol, idx) {
    return _c('option', {
      key: idx,
      domProps: {
        "value": idx
      }
    }, [_vm._v("\n                                    " + _vm._s(_vm.etiquetaOpcionPoliza(pol)) + "\n                                ")]);
  }), 0)], 2) : _vm._e(), _vm._ssrNode(" <div class=\"portal-empresa__poliza-grid\" data-v-7141faa9><div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.etiquetaNumeroPoliza)) + "</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.valorNumeroPoliza)) + "</p></div> <div class=\"portal-empresa__field\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>Compañía</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.companiaActual)) + "</p></div> <div class=\"portal-empresa__field portal-empresa__field--wide\" data-v-7141faa9><span class=\"portal-empresa__label\" data-v-7141faa9>Vigencia</span> <p class=\"portal-empresa__value\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.vigenciaActual)) + "</p></div></div> " + (_vm.pdfUrlActual ? "<div class=\"portal-empresa__pdf-row\" data-v-7141faa9><a" + _vm._ssrAttr("href", _vm.pdfUrlActual) + " target=\"_blank\" rel=\"noopener noreferrer\" class=\"portal-empresa__btn-pdf\" data-v-7141faa9><i class=\"fas fa-file-pdf\" data-v-7141faa9></i>\n                                Descargar PDF\n                            </a></div>" : "<!---->"))], 2) : _vm.tabsDisponibles.length ? _vm._ssrNode("<p class=\"portal-empresa__hint\" data-v-7141faa9>\n                        No hay pólizas en el ramo seleccionado.\n                    </p>") : _vm._ssrNode("<p class=\"portal-empresa__hint\" data-v-7141faa9>\n                        Aún no hay pólizas asociadas a este RFC. Si necesitas ayuda, contacta a tu ejecutivo.\n                    </p>"), _vm._ssrNode(" "), _vm._ssrNode("<div class=\"portal-empresa__link-wrap\" data-v-7141faa9>", "</div>", [_c('nuxt-link', {
    staticClass: "portal-empresa__link-portfolio",
    attrs: {
      "to": "/portafolio-polizas"
    }
  }, [_vm._v("\n                            Ver portafolio completo de pólizas\n                            "), _c('i', {
    staticClass: "fas fa-arrow-right"
  })])], 1)]], 2), _vm._ssrNode(" <div class=\"portal-empresa__card\" data-v-7141faa9><div class=\"portal-empresa__seg-toggle\" data-v-7141faa9><button type=\"button\"" + _vm._ssrClass("portal-empresa__seg-btn", {
    'portal-empresa__seg-btn--on': _vm.vistaInferior === 'asegurados'
  }) + " data-v-7141faa9>\n                        Asegurados\n                    </button> <button type=\"button\"" + _vm._ssrClass("portal-empresa__seg-btn", {
    'portal-empresa__seg-btn--on': _vm.vistaInferior === 'endosos'
  }) + " data-v-7141faa9>\n                        Endosos\n                    </button></div> " + (_vm.vistaInferior === 'asegurados' ? "<div class=\"portal-empresa__search\" data-v-7141faa9><i class=\"fas fa-search\" data-v-7141faa9></i> <input type=\"search\" placeholder=\"Buscar por nombre o RFC…\" autocomplete=\"off\"" + _vm._ssrAttr("value", _vm.busquedaAsegurado) + " class=\"portal-empresa__search-input\" data-v-7141faa9></div> " + (!_vm.aseguradosFiltrados.length ? "<div class=\"portal-empresa__empty-table\" data-v-7141faa9>\n                        No hay asegurados para mostrar. Comprueba que en Firestore las pólizas tengan\n                        <strong data-v-7141faa9>nombreEmpresa</strong> o <strong data-v-7141faa9>rfc</strong> (RFC de la empresa) alineados con tu cuenta,\n                        y que exista <strong data-v-7141faa9>dataAsegurados</strong> (o nombre/RFC del titular) con los integrantes.\n                        Ajusta la búsqueda o recarga la página.\n                    </div>" : "<div class=\"portal-empresa__table-wrap\" data-v-7141faa9><table class=\"portal-empresa__table\" data-v-7141faa9><thead data-v-7141faa9><tr data-v-7141faa9><th data-v-7141faa9>Asegurado</th> <th data-v-7141faa9>RFC</th> <th data-v-7141faa9>Ramo</th> <th data-v-7141faa9>Acción</th></tr></thead> <tbody data-v-7141faa9>" + _vm._ssrList(_vm.aseguradosPagina, function (row, idx) {
    return "<tr data-v-7141faa9><td data-v-7141faa9>" + _vm._ssrEscape(_vm._s(row.nombre)) + "</td> <td data-v-7141faa9>" + _vm._ssrEscape(_vm._s(row.rfc)) + "</td> <td data-v-7141faa9>" + _vm._ssrEscape(_vm._s(row.tipo)) + "</td> <td data-v-7141faa9><button type=\"button\" class=\"portal-empresa__btn-ver\" data-v-7141faa9><i class=\"fas fa-eye\" data-v-7141faa9></i>\n                                            Ver detalle\n                                        </button></td></tr>";
  }) + "</tbody></table></div>") + " " + (_vm.totalPaginas > 1 ? "<div class=\"portal-empresa__pager\" data-v-7141faa9><button type=\"button\"" + _vm._ssrAttr("disabled", _vm.paginaActual <= 1) + " class=\"portal-empresa__page-btn\" data-v-7141faa9>\n                            ‹\n                        </button> <span class=\"portal-empresa__page-info\" data-v-7141faa9>" + _vm._ssrEscape(_vm._s(_vm.paginaActual) + " / " + _vm._s(_vm.totalPaginas)) + "</span> <button type=\"button\"" + _vm._ssrAttr("disabled", _vm.paginaActual >= _vm.totalPaginas) + " class=\"portal-empresa__page-btn\" data-v-7141faa9>\n                            ›\n                        </button></div>" : "<!---->") : "<div class=\"portal-empresa__endosos-placeholder\" data-v-7141faa9><i class=\"fas fa-file-contract portal-empresa__endosos-icon\" data-v-7141faa9></i> <p data-v-7141faa9>\n                        La consulta de endosos estará disponible próximamente. Para trámites urgentes,\n                        contacta a tu ejecutivo IBS.\n                    </p></div>") + "</div>")], 2)]), _vm._ssrNode(" "), _c('transition', {
    attrs: {
      "name": "portal-empresa-modal-fade"
    }
  }, [_vm.modalDetalle ? _c('div', {
    staticClass: "portal-empresa-modal",
    attrs: {
      "role": "dialog",
      "aria-modal": "true"
    },
    on: {
      "click": function ($event) {
        if ($event.target !== $event.currentTarget) return null;
        return _vm.cerrarModalDetalle.apply(null, arguments);
      }
    }
  }, [_c('div', {
    staticClass: "portal-empresa-modal__box"
  }, [_c('header', {
    staticClass: "portal-empresa-modal__head"
  }, [_c('div', [_c('h2', {
    staticClass: "portal-empresa-modal__title"
  }, [_vm._v(_vm._s(_vm.modalNombre || 'Asegurado'))]), _vm._v(" "), _c('p', {
    staticClass: "portal-empresa-modal__rfc"
  }, [_vm._v("RFC: " + _vm._s(_vm.modalRfc))])]), _vm._v(" "), _c('button', {
    staticClass: "portal-empresa-modal__close",
    attrs: {
      "type": "button",
      "aria-label": "Cerrar"
    },
    on: {
      "click": _vm.cerrarModalDetalle
    }
  }, [_vm._v("\n                        ×\n                    ")])]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__body"
  }, [_c('p', {
    staticClass: "portal-empresa-modal__ramo-label"
  }, [_vm._v("RAMO")]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__tabs"
  }, _vm._l(_vm.modalTabsRamo, function (tab) {
    return _c('button', {
      key: tab.key,
      staticClass: "portal-empresa-modal__tab",
      class: {
        'portal-empresa-modal__tab--on': _vm.ramoModalActivo === tab.key
      },
      attrs: {
        "type": "button"
      },
      on: {
        "click": function ($event) {
          _vm.ramoModalActivo = tab.key;
        }
      }
    }, [_vm._v("\n                            " + _vm._s(tab.label) + "\n                        ")]);
  }), 0), _vm._v(" "), _vm.polizaModalActiva ? _c('div', {
    staticClass: "portal-empresa-modal__card"
  }, [_c('div', {
    staticClass: "portal-empresa-modal__fields"
  }, [_c('div', {
    staticClass: "portal-empresa-modal__field"
  }, [_c('span', {
    staticClass: "portal-empresa-modal__flabel"
  }, [_vm._v("Núm. póliza")]), _vm._v(" "), _c('span', {
    staticClass: "portal-empresa-modal__fval"
  }, [_vm._v(_vm._s(_vm.numPolizaModal))])]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__field"
  }, [_c('span', {
    staticClass: "portal-empresa-modal__flabel"
  }, [_vm._v("Compañía")]), _vm._v(" "), _c('span', {
    staticClass: "portal-empresa-modal__fval"
  }, [_vm._v(_vm._s(_vm.companiaModal))])]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__field"
  }, [_c('span', {
    staticClass: "portal-empresa-modal__flabel"
  }, [_vm._v("Vigencia desde")]), _vm._v(" "), _c('span', {
    staticClass: "portal-empresa-modal__fval"
  }, [_vm._v(_vm._s(_vm.vigenciaDesdeModal))])]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__field"
  }, [_c('span', {
    staticClass: "portal-empresa-modal__flabel"
  }, [_vm._v("Vigencia hasta")]), _vm._v(" "), _c('span', {
    staticClass: "portal-empresa-modal__fval"
  }, [_vm._v(_vm._s(_vm.vigenciaHastaModal))])])]), _vm._v(" "), _c('div', {
    staticClass: "portal-empresa-modal__actions"
  }, [_vm.pdfUrlModal ? _c('a', {
    staticClass: "portal-empresa-modal__btn-dl",
    attrs: {
      "href": _vm.pdfUrlModal,
      "target": "_blank",
      "rel": "noopener noreferrer"
    }
  }, [_c('i', {
    staticClass: "fas fa-download"
  }), _vm._v("\n                                Descargar póliza\n                            ")]) : _vm._e(), _vm._v(" "), _c('button', {
    staticClass: "portal-empresa-modal__btn-sec",
    attrs: {
      "type": "button"
    },
    on: {
      "click": _vm.irAPaginaPolizaCompleta
    }
  }, [_vm._v("\n                                Ver ficha completa\n                            ")])])]) : _c('p', {
    staticClass: "portal-empresa-modal__empty"
  }, [_vm._v("\n                        No hay datos de póliza en este ramo para este RFC.\n                    ")])])])]) : _vm._e()])], 2);
};

var staticRenderFns = [];

// CONCATENATED MODULE: ./components/PortalEmpresaView.vue?vue&type=template&id=7141faa9&scoped=true&

// CONCATENATED MODULE: ./lib/cargarPolizasPorRfc.js
/**
 * Carga pólizas locales (Firestore) + SICAS para un RFC dado.
 * Misma lógica que en pages/portafolio-polizas.vue
 */
const BASE_SICAS = 'https://api-sicas-616002718679.us-central1.run.app/api';

function mapSicasAutoToUI(p) {
  const concepto = (p.concepto || '').toString();
  const modelo = extraerModeloDesdeConcepto(concepto);
  return {
    descripcionAuto: concepto || `Póliza ${p.documento || ''}`.trim(),
    modelo: modelo || '',
    numPoliza: p.documento || '',
    concepto: concepto || '',
    idDocto: p.id,
    ciaNombre: p.ciaNombre,
    subRamo: p.subRamo,
    IDSRamo: p.IDSRamo,
    rfc: p.rfc,
    polizaDesde: p.polizaDesde,
    polizaHasta: p.polizaHasta,
    status: p.status,
    formaPago: p.formaPago,
    primaTotal: p.primaTotal,
    _origen: 'sicas',
    _rawSicas: p
  };
}

function mapSicasGmmToUI(p) {
  return {
    numPolizaGM: p.documento || '',
    idDocto: p.id,
    nombre: p.nombre,
    ciaNombre: p.ciaNombre,
    subRamo: p.subRamo,
    IDSRamo: p.IDSRamo,
    rfc: p.rfc,
    polizaDesde: p.polizaDesde,
    polizaHasta: p.polizaHasta,
    status: p.status,
    formaPago: p.formaPago,
    primaTotal: p.primaTotal,
    coaseguro: p.coaseguro,
    deducible: p.deducible,
    _origen: 'sicas',
    _rawSicas: p
  };
}

function mapSicasVidaToUI(p) {
  return {
    numPolizaVida: p.documento || '',
    certificadoVida: '',
    idDocto: p.id,
    nombre: p.nombre,
    ciaNombre: p.ciaNombre,
    subRamo: p.subRamo,
    IDSRamo: p.IDSRamo,
    rfc: p.rfc,
    polizaDesde: p.polizaDesde,
    polizaHasta: p.polizaHasta,
    status: p.status,
    formaPago: p.formaPago,
    primaTotal: p.primaTotal,
    _origen: 'sicas',
    _rawSicas: p
  };
}

function mapSicasHogarToUI(p) {
  return {
    numPolizaHogar: p.documento || '',
    idDocto: p.id,
    nombre: p.nombre,
    ciaNombre: p.ciaNombre,
    subRamo: p.subRamo,
    IDSRamo: p.IDSRamo,
    rfc: p.rfc,
    polizaDesde: p.polizaDesde,
    polizaHasta: p.polizaHasta,
    status: p.status,
    formaPago: p.formaPago,
    primaTotal: p.primaTotal,
    descripcionHogar: p.concepto || '',
    _origen: 'sicas',
    _rawSicas: p
  };
}

function extraerModeloDesdeConcepto(concepto) {
  const m = concepto.match(/\b(19\d{2}|20\d{2})\b/);
  return m ? m[1] : '';
}

function ordenarPorOrigen(arr) {
  return [...arr].sort((a, b) => {
    const oa = a._origen === 'sicas' ? 0 : 1;
    const ob = b._origen === 'sicas' ? 0 : 1;
    return oa - ob;
  });
}

function filtrarVigentesHogar(arr) {
  if (!Array.isArray(arr)) return [];
  const hoy = new Date();

  const parseFecha = valor => {
    if (!valor) return null;
    if (valor instanceof Date) return valor;
    const str = String(valor).trim();

    if (/^\d{4}-\d{2}-\d{2}/.test(str)) {
      const d = new Date(str);
      return Number.isNaN(d.getTime()) ? null : d;
    }

    const m = str.match(/^(\d{2})[\/-](\d{2})[\/-](\d{4})$/);

    if (m) {
      const [, dd, mm, yyyy] = m;
      const d = new Date(Number(yyyy), Number(mm) - 1, Number(dd));
      return Number.isNaN(d.getTime()) ? null : d;
    }

    const d = new Date(str);
    return Number.isNaN(d.getTime()) ? null : d;
  };

  return arr.filter(p => {
    const status = (p.status || p.estatus || '').toString().toLowerCase().trim();

    if (status && status !== 'vigente' && status !== 'activa') {
      return false;
    }

    const fin = p.vHasta || p.polizaHasta;
    if (!fin) return true;
    const fechaFin = parseFecha(fin);
    if (!fechaFin) return true;
    return fechaFin >= hoy;
  });
}

async function fetchPolizasSicas(rfc) {
  const out = {
    auto: [],
    gmm: [],
    vida: [],
    hogar: []
  };

  try {
    const resp = await fetch(`${BASE_SICAS}/polizas?rfc=${encodeURIComponent(rfc)}`);
    if (!resp.ok) return out;
    let data = await resp.json();

    if (!data || data.success === false) {
      data = null;
    }

    if (data) {
      out.auto = Array.isArray(data.autos) ? data.autos : [];
      out.gmm = Array.isArray(data.gmm) ? data.gmm : [];
      out.vida = Array.isArray(data.vida) ? data.vida : [];
      out.hogar = Array.isArray(data.hogar) ? data.hogar : [];
    }

    if (!out.gmm.length) {
      const gmmResp = await fetch(`${BASE_SICAS}/polizas/gmm?rfc=${encodeURIComponent(rfc)}`);

      if (gmmResp.ok) {
        const gmmData = await gmmResp.json();

        if (gmmData && gmmData.success !== false && Array.isArray(gmmData.gmm)) {
          out.gmm = gmmData.gmm;
        }
      }
    }
  } catch (e) {
    console.warn('SICAS fetch', e);
  }

  return out;
}
/**
 * @param {Object} opts
 * @param {import('firebase/firestore').Firestore} opts.db
 * @param {string} opts.rfc
 */


async function cargarPolizasPorRfc({
  db,
  rfc
}) {
  const polizas = {
    auto: [],
    gastosMedicos: [],
    vida: [],
    funerarias: [],
    accidentes: [],
    hogar: []
  };
  const promises = [];
  promises.push(db.collection('PolizaAutos').where('rfc', '==', rfc).get().then(snap => {
    polizas.auto.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(db.collection('PolizaGM').where('rfc', '==', rfc).get().then(snap => {
    polizas.gastosMedicos.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(db.collection('PolizaVida').where('rfc', '==', rfc).get().then(snap => {
    polizas.vida.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(db.collection('PolizaFuneraria').where('rfc', '==', rfc).get().then(snap => {
    polizas.funerarias.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(db.collection('PolizaAccidentes').where('rfc', '==', rfc).get().then(snap => {
    polizas.accidentes.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(db.collection('PolizaHogar').where('rfc', '==', rfc).get().then(snap => {
    polizas.hogar.push(...snap.docs.map(doc => ({ ...doc.data(),
      _origen: 'local'
    })));
  }));
  promises.push(fetchPolizasSicas(rfc).then(sicas => {
    polizas.auto.push(...sicas.auto.map(mapSicasAutoToUI));
    polizas.gastosMedicos.push(...sicas.gmm.map(mapSicasGmmToUI));
    polizas.vida.push(...sicas.vida.map(mapSicasVidaToUI));
    polizas.hogar.push(...sicas.hogar.map(mapSicasHogarToUI));
  }));
  await Promise.all(promises);
  polizas.hogar = filtrarVigentesHogar(polizas.hogar);
  polizas.auto = ordenarPorOrigen(polizas.auto);
  polizas.gastosMedicos = ordenarPorOrigen(polizas.gastosMedicos);
  polizas.vida = ordenarPorOrigen(polizas.vida);
  polizas.hogar = ordenarPorOrigen(polizas.hogar);
  return polizas;
}
// CONCATENATED MODULE: ./lib/imgEmpresa.js
/**
 * Colección Firestore `ImgEmpresa`: configura qué ramos / pólizas están dados de alta
 * para una empresa identificada por `nombreEmpresa` (debe alinearse con `Usuarios.nombre`).
 *
 * Los documentos pueden incluir mapas anidados por ramo, p. ej.:
 * `polizaAccidentes`, `polizaFuneraria`, `polizaGM`, `polizaVida`, `polizaAuto`, `polizaHogar`
 * con campos tipo: `compania`, `file_url`, `numPoliza*`, `vDesde`, `vHasta`.
 */
const RAMO_KEYS = ['gmm', 'vida', 'funeraria', 'accidentes', 'auto', 'hogar'];
/** Convierte Timestamp de Firestore / Date / string a valor usable en la UI. */

function aplanarFechaCampo(val) {
  if (val == null || val === '') return val;
  if (typeof val.toDate === 'function') return val.toDate();

  if (val && typeof val.seconds === 'number') {
    return new Date(val.seconds * 1000);
  }

  if (val instanceof Date) return val;
  return val;
}
/**
 * Determina si un mapa de póliza anidado tiene al menos un dato útil (no todo null).
 * @param {Object|null|undefined} m
 */

function mapaPolizaTieneDatos(m) {
  if (!m || typeof m !== 'object') return false;

  for (const v of Object.values(m)) {
    if (v == null || v === '') continue;

    if (typeof v === 'object') {
      if (typeof v.toDate === 'function') return true;
      if (v.seconds != null) return true;
      if (v instanceof Date) return true;
      continue;
    }

    return true;
  }

  return false;
}

function mapAnidadoAGmm(raw) {
  const r = raw || {};
  return {
    numPolizaGM: r.numPolizaGM || r.numPolizaGMM || r.documento || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    nombre: r.nombre || '',
    _origen: 'imgEmpresa'
  };
}

function mapAnidadoAVida(raw) {
  const r = raw || {};
  return {
    numPolizaVida: r.numPolizaVida || r.documento || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    nombre: r.nombre || '',
    _origen: 'imgEmpresa'
  };
}

function mapAnidadoAFuneraria(raw) {
  const r = raw || {};
  return {
    numPolizaFuneraria: r.numPolizaFuneraria || r.documento || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    nombre: r.nombre || '',
    _origen: 'imgEmpresa'
  };
}

function mapAnidadoAAccidentes(raw) {
  const r = raw || {};
  return {
    numPolizaAccidente: r.numPolizaAccidente || r.documento || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    nombre: r.nombre || '',
    _origen: 'imgEmpresa'
  };
}

function mapAnidadoAAuto(raw) {
  const r = raw || {};
  return {
    numPoliza: r.numPoliza || r.documento || '',
    descripcionAuto: r.descripcionAuto || r.concepto || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    _origen: 'imgEmpresa'
  };
}

function mapAnidadoAHogar(raw) {
  const r = raw || {};
  return {
    numPolizaHogar: r.numPolizaHogar || r.documento || '',
    descripcionHogar: r.descripcionHogar || '',
    ciaNombre: r.compania != null ? String(r.compania) : '',
    file_url: r.file_url || '',
    polizaDesde: aplanarFechaCampo(r.vDesde),
    polizaHasta: aplanarFechaCampo(r.vHasta),
    vDesde: aplanarFechaCampo(r.vDesde),
    vHasta: aplanarFechaCampo(r.vHasta),
    nombre: r.nombre || '',
    _origen: 'imgEmpresa'
  };
}
/**
 * Lee los mapas anidados del documento ImgEmpresa y devuelve el mismo shape que `cargarPolizasPorRfc`
 * (solo ramos con datos en el documento).
 * @param {Object|null} data
 * @returns {Object|null} null si no hay mapas con datos; si no, objeto con arrays por ramo
 */


function extraerPolizasDesdeMapasAnidadosImgEmpresa(data) {
  if (!data || typeof data !== 'object') return null;
  /** @type {Record<string, Array<{ field: string, map: Function }>>} */

  const definiciones = {
    gastosMedicos: [{
      field: 'polizaGM',
      map: mapAnidadoAGmm
    }, {
      field: 'polizaGMM',
      map: mapAnidadoAGmm
    }, {
      field: 'polizaGastosMedicos',
      map: mapAnidadoAGmm
    }],
    vida: [{
      field: 'polizaVida',
      map: mapAnidadoAVida
    }],
    funerarias: [{
      field: 'polizaFuneraria',
      map: mapAnidadoAFuneraria
    }],
    accidentes: [{
      field: 'polizaAccidentes',
      map: mapAnidadoAAccidentes
    }],
    auto: [{
      field: 'polizaAuto',
      map: mapAnidadoAAuto
    }, {
      field: 'polizaAutos',
      map: mapAnidadoAAuto
    }],
    hogar: [{
      field: 'polizaHogar',
      map: mapAnidadoAHogar
    }]
  };
  const out = {
    gastosMedicos: [],
    vida: [],
    funerarias: [],
    accidentes: [],
    auto: [],
    hogar: []
  };
  let alguno = false;

  for (const ramo of Object.keys(definiciones)) {
    for (const {
      field,
      map
    } of definiciones[ramo]) {
      const rawMap = data[field];
      if (!mapaPolizaTieneDatos(rawMap)) continue;
      out[ramo].push(map(rawMap));
      alguno = true;
      break;
    }
  }

  return alguno ? out : null;
}
/**
 * Sustituye en `merged` los ramos para los que exista dato en mapas anidados ImgEmpresa
 * (los mapas del documento tienen prioridad sobre RFC/SICAS).
 */

function aplicarPolizasAnidadasImgEmpresa(merged, data) {
  const extraidas = extraerPolizasDesdeMapasAnidadosImgEmpresa(data);
  if (!extraidas) return merged;
  const out = { ...merged
  };
  if (extraidas.gastosMedicos.length) out.gastosMedicos = extraidas.gastosMedicos;
  if (extraidas.vida.length) out.vida = extraidas.vida;
  if (extraidas.funerarias.length) out.funerarias = extraidas.funerarias;
  if (extraidas.accidentes.length) out.accidentes = extraidas.accidentes;
  if (extraidas.auto.length) out.auto = extraidas.auto;
  if (extraidas.hogar.length) out.hogar = extraidas.hogar;
  return out;
}
/** Normaliza texto para comparar nombres de empresa sin acentos ni mayúsculas. */

function normalizarTextoEmpresa(s) {
  if (s == null || s === '') return '';
  return String(s).trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '');
}
/**
 * Convierte una etiqueta humana o de Firebase al key interno del portal.
 * @param {string} raw
 * @returns {string|null}
 */

function normalizarRamoKey(raw) {
  if (raw == null || raw === '') return null;
  const x = String(raw).trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '');

  if (['gmm', 'gm', 'gastos medicos', 'gastosmedicos', 'gastos_medicos', 'gmm colectivo'].includes(x)) {
    return 'gmm';
  }

  if (['vida', 'seguro de vida'].includes(x)) return 'vida';
  if (['funeraria', 'funerario', 'funerarios'].includes(x)) return 'funeraria';
  if (['accidentes', 'accidente', 'accidentes personales'].includes(x)) return 'accidentes';
  if (['auto', 'autos', 'automovil', 'automoviles', 'automóvil'].includes(x)) return 'auto';
  if (['hogar', 'daños', 'danos'].includes(x)) return 'hogar';
  return null;
}
/**
 * Interpreta el documento `ImgEmpresa` y devuelve un Set de keys de ramo permitidos,
 * o `null` si no hay criterio explícito (se muestran todos los datos cargados por RFC).
 *
 * Campos soportados:
 * - Arrays: `ramos`, `ramosActivos`, `ramosRegistrados`, `polizasActivas`
 * - Booleanos: `gmm`, `GMM`, `vida`, `Vida`, `funeraria`, `accidentes`, `auto`, `hogar`, etc.
 *
 * @param {Object|null} data
 * @returns {Set<string>|null}
 */

function parseRamosDesdeImgEmpresa(data) {
  if (!data || typeof data !== 'object') return null;
  const set = new Set();

  for (const field of ['ramos', 'ramosActivos', 'ramosRegistrados', 'polizasActivas', 'ramosDadosDeAlta']) {
    if (Array.isArray(data[field])) {
      for (const item of data[field]) {
        const k = normalizarRamoKey(item);
        if (k) set.add(k);
      }
    }
  }

  const boolFields = [['gmm', ['gmm', 'GMM', 'gastosMedicos', 'GastosMedicos', 'GastosMedicosMayores', 'polizaGMM', 'polizaGM']], ['vida', ['vida', 'Vida', 'polizaVida']], ['funeraria', ['funeraria', 'Funeraria', 'polizaFuneraria']], ['accidentes', ['accidentes', 'Accidentes', 'polizaAccidentes']], ['auto', ['auto', 'Auto', 'autos', 'polizaAuto', 'PolizaAutos']], ['hogar', ['hogar', 'Hogar', 'polizaHogar']]];

  for (const [key, names] of boolFields) {
    for (const n of names) {
      if (data[n] === true) set.add(key);
    }
  }

  const desdeMapas = extraerPolizasDesdeMapasAnidadosImgEmpresa(data);

  if (desdeMapas) {
    if (desdeMapas.gastosMedicos.length) set.add('gmm');
    if (desdeMapas.vida.length) set.add('vida');
    if (desdeMapas.funerarias.length) set.add('funeraria');
    if (desdeMapas.accidentes.length) set.add('accidentes');
    if (desdeMapas.auto.length) set.add('auto');
    if (desdeMapas.hogar.length) set.add('hogar');
  }

  if (set.size) return set;
  return null;
}
/**
 * Si el documento lista números de póliza por ramo, filtra cada arreglo.
 * Ej.: `numerosGMM: ['123','456']` o `polizasGMM: [...]`
 *
 * @param {Object} polizas - mismo shape que cargarPolizasPorRfc
 * @param {Object} data - datos ImgEmpresa
 */

function filtrarPolizasPorNumerosOpcional(polizas, data) {
  if (!data || typeof data !== 'object') return polizas;

  const pickNums = keys => {
    for (const k of keys) {
      const v = data[k];

      if (Array.isArray(v) && v.length) {
        return new Set(v.map(x => String(x).trim()));
      }
    }

    return null;
  };

  const norm = (p, getters) => {
    for (const g of getters) {
      const v = g(p);
      if (v != null && v !== '') return String(v).trim();
    }

    return '';
  };

  const out = { ...polizas
  };
  const numsGmm = pickNums(['numerosGMM', 'numerosPolizaGMM', 'polizasGMM', 'numPolizaGMM']);

  if (numsGmm && numsGmm.size) {
    out.gastosMedicos = polizas.gastosMedicos.filter(p => numsGmm.has(norm(p, [x => x.numPolizaGM, x => x.documento])));
  }

  const numsVida = pickNums(['numerosVida', 'polizasVida', 'numPolizaVida']);

  if (numsVida && numsVida.size) {
    out.vida = polizas.vida.filter(p => numsVida.has(norm(p, [x => x.numPolizaVida, x => x.documento])));
  }

  const numsFun = pickNums(['numerosFuneraria', 'polizasFuneraria']);

  if (numsFun && numsFun.size) {
    out.funerarias = polizas.funerarias.filter(p => numsFun.has(norm(p, [x => x.numPolizaFuneraria, x => x.documento])));
  }

  const numsAcc = pickNums(['numerosAccidentes', 'polizasAccidentes']);

  if (numsAcc && numsAcc.size) {
    out.accidentes = polizas.accidentes.filter(p => numsAcc.has(norm(p, [x => x.numPolizaAccidente, x => x.documento])));
  }

  const numsAuto = pickNums(['numerosAuto', 'polizasAuto']);

  if (numsAuto && numsAuto.size) {
    out.auto = polizas.auto.filter(p => numsAuto.has(norm(p, [x => x.numPoliza, x => x.documento])));
  }

  const numsHogar = pickNums(['numerosHogar', 'polizasHogar']);

  if (numsHogar && numsHogar.size) {
    out.hogar = (polizas.hogar || []).filter(p => numsHogar.has(norm(p, [x => x.numPolizaHogar, x => x.documento])));
  }

  return out;
}
/**
 * Deja solo los ramos indicados en `ramosPermitidos`. Si `ramosPermitidos` es null, no filtra por ramo.
 * @param {Object} polizas
 * @param {Set<string>|null} ramosPermitidos
 */

function filtrarPolizasPorRamos(polizas, ramosPermitidos) {
  if (!ramosPermitidos || ramosPermitidos.size === 0) return polizas;
  const out = { ...polizas
  };
  if (!ramosPermitidos.has('gmm')) out.gastosMedicos = [];
  if (!ramosPermitidos.has('vida')) out.vida = [];
  if (!ramosPermitidos.has('funeraria')) out.funerarias = [];
  if (!ramosPermitidos.has('accidentes')) out.accidentes = [];
  if (!ramosPermitidos.has('auto')) out.auto = [];
  if (!ramosPermitidos.has('hogar')) out.hogar = [];
  return out;
}
/**
 * Busca un documento en `ImgEmpresa` cuyo `nombreEmpresa` coincida con el del usuario.
 * Intenta coincidencia exacta y variantes de mayúsculas; si falla, compara nombres normalizados (máx. 200 docs).
 *
 * @param {import('firebase/firestore').Firestore} db
 * @param {string} nombreEmpresa - típicamente `Usuarios.nombre`
 * @returns {Promise<{ data: Object|null, id: string|null }>}
 */

async function fetchImgEmpresaPorNombre(db, nombreEmpresa) {
  const raw = String(nombreEmpresa || '').trim();
  if (!raw) return {
    data: null,
    id: null
  };
  const variants = [...new Set([raw, raw.toUpperCase(), raw.toLowerCase()])];

  for (const v of variants) {
    const snap = await db.collection('ImgEmpresa').where('nombreEmpresa', '==', v).limit(3).get();

    if (!snap.empty) {
      const doc = snap.docs[0];
      return {
        data: doc.data(),
        id: doc.id
      };
    }
  }

  try {
    const snap = await db.collection('ImgEmpresa').limit(200).get();
    const target = normalizarTextoEmpresa(raw);

    for (const doc of snap.docs) {
      const d = doc.data();
      const ne = d && d.nombreEmpresa != null ? String(d.nombreEmpresa) : '';

      if (ne && normalizarTextoEmpresa(ne) === target) {
        return {
          data: d,
          id: doc.id
        };
      }
    }
  } catch (e) {
    console.warn('ImgEmpresa: no se pudo leer colección para coincidencia flexible', e);
  }

  return {
    data: null,
    id: null
  };
}
/**
 * Aplica reglas ImgEmpresa sobre el resultado de `cargarPolizasPorRfc`.
 * @returns {{ polizas: Object, ramosPermitidos: Set<string>|null, imgEmpresa: Object|null, imgEmpresaId: string|null }}
 */

async function aplicarReglasImgEmpresa(db, nombreEmpresa, polizasCompletas) {
  const {
    data,
    id
  } = await fetchImgEmpresaPorNombre(db, nombreEmpresa);

  if (!data) {
    return {
      polizas: polizasCompletas,
      ramosPermitidos: null,
      imgEmpresa: null,
      imgEmpresaId: null
    };
  }

  const ramos = parseRamosDesdeImgEmpresa(data);
  let merged = { ...polizasCompletas
  };
  merged = filtrarPolizasPorNumerosOpcional(merged, data);

  if (ramos && ramos.size) {
    merged = filtrarPolizasPorRamos(merged, ramos);
  } // Mapas anidados en el documento (p. ej. polizaAccidentes) sustituyen las pólizas cargadas por RFC.


  merged = aplicarPolizasAnidadasImgEmpresa(merged, data);
  return {
    polizas: merged,
    ramosPermitidos: ramos,
    imgEmpresa: data,
    imgEmpresaId: id
  };
}

// CONCATENATED MODULE: ./lib/aseguradosPorEmpresa.js
/**
 * Consulta pólizas en Firestore por empresa (nombreEmpresa y/o RFC empresa)
 * y extrae filas de asegurados con campos flexibles.
 */

const COLECCIONES = [{
  collection: 'PolizaGM',
  tipo: 'GMM',
  ramo: 'gmm'
}, {
  collection: 'PolizaVida',
  tipo: 'Vida',
  ramo: 'vida'
}, {
  collection: 'PolizaFuneraria',
  tipo: 'Funeraria',
  ramo: 'funeraria'
}, {
  collection: 'PolizaAccidentes',
  tipo: 'Accidentes',
  ramo: 'accidentes'
}, {
  collection: 'PolizaAutos',
  tipo: 'Auto',
  ramo: 'auto'
}, {
  collection: 'PolizaHogar',
  tipo: 'Hogar',
  ramo: 'hogar'
}];
/** Variantes de RFC para coincidir con Firestore (con/sin guiones, mayúsculas). */

function variantesRfc(rfc) {
  const s = String(rfc || '').trim();
  if (!s) return [];
  const sinGuion = s.replace(/-/g, '').toUpperCase();
  return [...new Set([s, s.toUpperCase(), s.toLowerCase(), sinGuion])].filter(Boolean);
}
function pickNombreDesdeItem(a) {
  if (a == null) return '';
  if (typeof a === 'string') return a.trim();

  if (typeof a === 'object') {
    return String(a.nombre || a.Nombre || a.nombreCompleto || a.nombreAsegurado || a.nomAsegurado || a.nombre_completo || (typeof a.datosAsegurado === 'string' ? a.datosAsegurado : '') || '').trim();
  }

  return '';
}
function pickRfcDesdeItem(a) {
  if (!a || typeof a !== 'object') return '';
  const r = a.rfc || a.RFC || a.rfcAsegurado || '';
  return String(r).replace(/\s/g, '').trim();
}

function pickNombreTitularPoliza(d) {
  if (!d || typeof d !== 'object') return '';
  return String(d.nombre || d.Nombre || d.nombreAsegurado || d.nombreTitular || d.nomTitular || '').trim();
}

function pickRfcTitularPoliza(d) {
  if (!d || typeof d !== 'object') return '';
  const r = d.rfc || d.RFC || d.rfcTitular || d.rfcAseguradoTitular || '';
  return String(r).replace(/\s/g, '').trim();
}
/**
 * Normaliza dataAsegurados: array, objeto único legacy o mapa de objetos (como en PolizaGM / GMM Colectivo).
 * @param {*} raw
 * @returns {Array}
 */


function normalizarListaAsegurados(raw) {
  if (raw == null) return [];
  if (Array.isArray(raw) && raw.length > 0) return raw;

  if (raw && typeof raw === 'object' && !Array.isArray(raw)) {
    if (raw.nombre || raw.datosAsegurado || raw.Nombre) return [raw];
    const nested = Object.values(raw).filter(item => item && typeof item === 'object');
    return nested.length > 0 ? nested : [];
  }

  return [];
}

function obtenerRawAsegurados(d) {
  if (!d || typeof d !== 'object') return null;
  const keys = ['dataAsegurados', 'asegurados', 'listaAsegurados', 'Asegurados', 'integrantes'];

  for (const k of keys) {
    if (d[k] != null) return d[k];
  }

  return null;
}
/**
 * Extrae filas { nombre, rfc, tipo, poliza } desde un objeto de póliza (Firestore o API).
 * Soporta dataAsegurados como array, objeto único o mapa; datosAsegurado string en raíz (legacy).
 * @param {Object} d - datos de la póliza
 * @param {string} tipo - etiqueta de ramo (GMM, Vida, …)
 * @param {Object} polizaRef - referencia que se guarda en cada fila (típicamente el mismo d)
 */


function extraerAseguradosDePolizaData(d, tipo, polizaRef) {
  const poliza = polizaRef || d;
  const rows = [];
  const rfcPoliza = d.rfc != null && String(d.rfc).trim() !== '' ? String(d.rfc).trim() : '—';
  const legacyNombre = typeof d.datosAsegurado === 'string' && d.datosAsegurado.trim() ? d.datosAsegurado.trim() : '';
  const rawA = obtenerRawAsegurados(d);
  const lista = normalizarListaAsegurados(rawA);

  if (lista.length) {
    lista.forEach(a => {
      const datosAsegurado = pickNombreDesdeItem(a) || (typeof (a === null || a === void 0 ? void 0 : a.datosAsegurado) === 'string' ? a.datosAsegurado.trim() : '') || legacyNombre || '—';
      const rfcFila = pickRfcDesdeItem(a) || (rfcPoliza !== '—' ? rfcPoliza : '');
      rows.push({
        nombre: String(datosAsegurado),
        rfc: rfcFila && rfcFila !== '' ? rfcFila : '—',
        tipo,
        poliza,
        origen: 'poliza'
      });
    });
    return rows;
  }

  if (legacyNombre) {
    rows.push({
      nombre: legacyNombre,
      rfc: rfcPoliza !== '—' ? rfcPoliza : '—',
      tipo,
      poliza,
      origen: 'poliza'
    });
    return rows;
  }

  const nombreTit = pickNombreTitularPoliza(d);
  const rfcTit = pickRfcTitularPoliza(d);

  if (nombreTit && rfcTit) {
    rows.push({
      nombre: nombreTit,
      rfc: rfcTit,
      tipo,
      poliza,
      origen: 'poliza'
    });
    return rows;
  }

  if (tipo === 'GMM') {
    rows.push({
      nombre: '—',
      rfc: rfcPoliza !== '—' ? rfcPoliza : '—',
      tipo,
      poliza,
      origen: 'poliza'
    });
  }

  return rows;
}
/**
 * @param {import('firebase/firestore').Firestore} db
 * @param {string} nombreEmpresa
 */

async function fetchDocumentosPorNombreEmpresa(db, nombreEmpresa) {
  const raw = String(nombreEmpresa || '').trim();
  if (!raw) return [];
  const variants = [...new Set([raw, raw.toUpperCase(), raw.toLowerCase()])];
  const out = [];
  const seenIds = new Set();

  for (const {
    collection,
    tipo,
    ramo
  } of COLECCIONES) {
    let matched = false;

    for (const v of variants) {
      try {
        const snap = await db.collection(collection).where('nombreEmpresa', '==', v).get();

        if (!snap.empty) {
          snap.docs.forEach(doc => {
            const key = `${collection}/${doc.id}`;
            if (seenIds.has(key)) return;
            seenIds.add(key);
            out.push({
              id: doc.id,
              collection,
              tipo,
              ramo,
              data: doc.data()
            });
          });
          matched = true;
          break;
        }
      } catch (e) {
        console.warn(`[${collection}] query nombreEmpresa`, e);
      }
    }

    if (matched) continue;

    try {
      const snap = await db.collection(collection).limit(500).get();
      const target = normalizarTextoEmpresa(raw);
      snap.docs.forEach(doc => {
        const d = doc.data();
        const ne = d.nombreEmpresa != null ? String(d.nombreEmpresa) : '';
        if (!ne || normalizarTextoEmpresa(ne) !== target) return;
        const key = `${collection}/${doc.id}`;
        if (seenIds.has(key)) return;
        seenIds.add(key);
        out.push({
          id: doc.id,
          collection,
          tipo,
          ramo,
          data: d
        });
      });
    } catch (e) {
      console.warn(`[${collection}] scan nombreEmpresa`, e);
    }
  }

  return out;
}
/**
 * Pólizas donde el campo `rfc` coincide con el RFC de la empresa (colectivo).
 * @param {import('firebase/firestore').Firestore} db
 * @param {string} rfcEmpresa
 */

async function fetchDocumentosPorRfcEmpresa(db, rfcEmpresa) {
  const raw = String(rfcEmpresa || '').trim();
  if (!raw) return [];
  const variants = variantesRfc(raw);
  const out = [];
  const seenIds = new Set();

  for (const {
    collection,
    tipo,
    ramo
  } of COLECCIONES) {
    let matched = false;

    for (const v of variants) {
      try {
        const snap = await db.collection(collection).where('rfc', '==', v).get();

        if (!snap.empty) {
          snap.docs.forEach(doc => {
            const key = `${collection}/${doc.id}`;
            if (seenIds.has(key)) return;
            seenIds.add(key);
            out.push({
              id: doc.id,
              collection,
              tipo,
              ramo,
              data: doc.data()
            });
          });
          matched = true;
          break;
        }
      } catch (e) {
        console.warn(`[${collection}] query rfc empresa`, e);
      }
    }

    if (matched) continue;

    try {
      const snap = await db.collection(collection).limit(500).get();
      const targets = new Set(variants.map(x => String(x).replace(/-/g, '').toUpperCase()));
      snap.docs.forEach(doc => {
        const d = doc.data();
        const r = d.rfc != null ? String(d.rfc).replace(/-/g, '').toUpperCase() : '';
        if (!r || !targets.has(r)) return;
        const key = `${collection}/${doc.id}`;
        if (seenIds.has(key)) return;
        seenIds.add(key);
        out.push({
          id: doc.id,
          collection,
          tipo,
          ramo,
          data: d
        });
      });
    } catch (e) {
      console.warn(`[${collection}] scan rfc`, e);
    }
  }

  return out;
}

function mergeDocumentosPorId(a, b) {
  const seen = new Set();
  const out = [];

  for (const d of [...a, ...b]) {
    const k = `${d.collection}/${d.id}`;
    if (seen.has(k)) continue;
    seen.add(k);
    out.push(d);
  }

  return out;
}
/**
 * PolizaGM donde el campo anidado ImgEmpresa.nombreEmpresa coincide con el nombre comercial
 * o con el id del documento en ImgEmpresa (mismo criterio que la vista GMM Colectivo).
 * @param {import('firebase/firestore').Firestore} db
 * @param {{ nombreEmpresa?: string, imgEmpresaDocId?: string|null }} opts
 */


async function fetchPolizaGMPorImgEmpresaAnidado(db, {
  nombreEmpresa,
  imgEmpresaDocId
}) {
  const criterios = [nombreEmpresa, imgEmpresaDocId].filter(v => v != null && String(v).trim() !== '');
  const unicos = [...new Set(criterios.map(s => String(s).trim()))];
  if (!unicos.length) return [];
  const out = [];
  const seenIds = new Set();

  for (const valor of unicos) {
    const variants = [...new Set([valor, valor.toUpperCase(), valor.toLowerCase()])];

    for (const v of variants) {
      try {
        const snap = await db.collection('PolizaGM').where('ImgEmpresa.nombreEmpresa', '==', v).get();
        snap.docs.forEach(doc => {
          const key = `PolizaGM/${doc.id}`;
          if (seenIds.has(key)) return;
          seenIds.add(key);
          out.push({
            id: doc.id,
            collection: 'PolizaGM',
            tipo: 'GMM',
            ramo: 'gmm',
            data: doc.data()
          });
        });
      } catch (e) {
        console.warn('[PolizaGM] query ImgEmpresa.nombreEmpresa', e);
      }
    }
  }

  return out;
}
/**
 * Une consultas por nombre de empresa, RFC de empresa y campo anidado ImgEmpresa en PolizaGM.
 * @param {{ nombre?: string, rfc?: string, imgEmpresaDocId?: string|null }} opts
 */

async function fetchTodosDocumentosEmpresa(db, {
  nombre,
  rfc,
  imgEmpresaDocId
}) {
  const [porNombre, porRfc, porNested] = await Promise.all([fetchDocumentosPorNombreEmpresa(db, nombre), fetchDocumentosPorRfcEmpresa(db, rfc), fetchPolizaGMPorImgEmpresaAnidado(db, {
    nombreEmpresa: nombre,
    imgEmpresaDocId
  })]);
  return mergeDocumentosPorId(mergeDocumentosPorId(porNombre, porRfc), porNested);
}

function claveUnicaAsegurado(nombre, rfc, tipo) {
  const n = normalizarTextoEmpresa(String(nombre || ''));
  const r = String(rfc || '').replace(/\s/g, '').toUpperCase();
  return `${n}|${r}|${tipo}`;
}
/**
 * @param {Array<{ id: string, collection: string, tipo: string, ramo: string, data: Object }>} documentos
 */


function extraerFilasAseguradosDesdeDocumentos(documentos) {
  const rows = [];
  const seen = new Set();

  for (const item of documentos) {
    const d = item.data;
    const tipo = item.tipo;
    const poliza = { ...d,
      _firestoreId: item.id,
      _collection: item.collection,
      _origen: 'empresaQuery'
    };
    const extraidas = extraerAseguradosDePolizaData(d, tipo, poliza);

    for (const row of extraidas) {
      row.origen = 'nombreEmpresa';
      const ck = claveUnicaAsegurado(row.nombre, row.rfc, row.tipo);
      if (seen.has(ck)) continue;
      seen.add(ck);
      rows.push(row);
    }
  }

  return rows;
}
/**
 * Combina listas evitando duplicados estrictos (nombre+rfc+tipo).
 */

function fusionarFilasAsegurados(base, extra) {
  const seen = new Set();
  const out = [];

  for (const row of base) {
    const ck = claveUnicaAsegurado(row.nombre, row.rfc, row.tipo);
    if (seen.has(ck)) continue;
    seen.add(ck);
    out.push(row);
  }

  for (const row of extra) {
    const ck = claveUnicaAsegurado(row.nombre, row.rfc, row.tipo);
    if (seen.has(ck)) continue;
    seen.add(ck);
    out.push(row);
  }

  return out;
}
/**
 * Una fila por persona (RFC físico). Si no hay RFC válido, se mantiene la fila.
 */

function dedupeAseguradosPorPersona(rows) {
  const porRfc = new Map();
  const sinClave = [];

  for (const row of rows) {
    const r = String(row.rfc || '').replace(/\s/g, '').toUpperCase();

    if (r && r !== '—' && r.length >= 10) {
      if (!porRfc.has(r)) porRfc.set(r, row);
      continue;
    }

    sinClave.push(row);
  }

  return [...porRfc.values(), ...sinClave];
}
// CONCATENATED MODULE: ./lib/imgCompaniaCatalog.js
/**
 * Catálogo ImgCompania: id de documento → nombre visible.
 * @param {import('firebase/firestore').Firestore} db
 * @returns {Promise<Map<string, string>>}
 */
async function fetchCatalogoCompaniasMap(db) {
  const m = new Map();
  if (!db) return m;

  try {
    const snap = await db.collection('ImgCompania').get();
    snap.docs.forEach(d => {
      const data = d.data() || {};
      m.set(d.id, (data.nombreCompania || d.id).toString());
    });
  } catch (e) {
    console.warn('[ImgCompania] fetchCatalogoCompaniasMap', e);
  }

  return m;
}
// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/PortalEmpresaView.vue?vue&type=script&lang=js&




const PAGE_SIZE = 10;
/* harmony default export */ var PortalEmpresaViewvue_type_script_lang_js_ = ({
  name: 'PortalEmpresaView',
  props: {
    empresa: {
      type: Object,
      required: true
    }
  },

  data() {
    return {
      polizas: {
        auto: [],
        gastosMedicos: [],
        vida: [],
        funerarias: [],
        accidentes: [],
        hogar: []
      },
      cargandoPolizas: true,
      errorPolizas: null,
      ramoActivo: 'gmm',
      indicePoliza: 0,
      vistaInferior: 'asegurados',
      busquedaAsegurado: '',
      paginaActual: 1,
      imgEmpresaRegistro: null,
      ramosImgEmpresa: null,
      sinRegistroImgEmpresa: false,
      perfilImgEmpresaResuelto: false,
      documentosPorEmpresa: [],

      /** Map id ImgCompania → nombreCompania */
      catalogoCompanias: null,
      modalDetalle: false,
      modalNombre: '',
      modalRfc: '',
      ramoModalActivo: 'gmm'
    };
  },

  computed: {
    imgEmpresaLogoUrl() {
      const d = this.imgEmpresaRegistro;
      if (!d) return '';
      return d.urlImagen || d.imagen || d.url || d.img || d.logo || d.logoUrl || '';
    },

    textoRamosImgEmpresa() {
      const labels = {
        gmm: 'GMM',
        vida: 'Vida',
        funeraria: 'Funeraria',
        accidentes: 'Accidentes',
        auto: 'Auto',
        hogar: 'Hogar'
      };
      if (!this.ramosImgEmpresa || !this.ramosImgEmpresa.size) return '';
      return [...this.ramosImgEmpresa].map(k => labels[k] || k).join(', ');
    },

    emailContactoMostrado() {
      const d = this.imgEmpresaRegistro;
      if (d && d.emailContacto) return d.emailContacto;
      return this.empresa.email || '—';
    },

    telefonoContactoMostrado() {
      const d = this.imgEmpresaRegistro;
      if (d && d.numTelefono) return d.numTelefono;
      return this.empresa.telefono || '—';
    },

    tabsDisponibles() {
      const t = [];
      const p = this.polizas;
      if (p.gastosMedicos.length) t.push({
        key: 'gmm',
        label: 'GMM'
      });
      if (p.vida.length) t.push({
        key: 'vida',
        label: 'Vida'
      });
      if (p.funerarias.length) t.push({
        key: 'funeraria',
        label: 'Funeraria'
      });
      if (p.accidentes.length) t.push({
        key: 'accidentes',
        label: 'Accidentes'
      });
      if (p.auto.length) t.push({
        key: 'auto',
        label: 'Auto'
      });
      if (p.hogar && p.hogar.length) t.push({
        key: 'hogar',
        label: 'Hogar'
      });
      return t;
    },

    listaPolizaActiva() {
      const p = this.polizas;

      switch (this.ramoActivo) {
        case 'gmm':
          return p.gastosMedicos;

        case 'vida':
          return p.vida;

        case 'funeraria':
          return p.funerarias;

        case 'accidentes':
          return p.accidentes;

        case 'auto':
          return p.auto;

        case 'hogar':
          return p.hogar || [];

        default:
          return [];
      }
    },

    polizaActiva() {
      const list = this.listaPolizaActiva;
      if (!list.length) return null;
      const i = Math.min(this.indicePoliza, list.length - 1);
      return list[i];
    },

    etiquetaNumeroPoliza() {
      switch (this.ramoActivo) {
        case 'gmm':
          return 'Número de póliza';

        case 'vida':
          return 'Número de póliza';

        case 'funeraria':
          return 'Número de póliza';

        case 'accidentes':
          return 'Número de póliza';

        case 'auto':
          return 'Póliza';

        case 'hogar':
          return 'Número de póliza';

        default:
          return 'Número de póliza';
      }
    },

    valorNumeroPoliza() {
      const pol = this.polizaActiva;
      if (!pol) return '—';

      if (this.ramoActivo === 'gmm') {
        return pol.numPolizaGM || pol.documento || '—';
      }

      if (this.ramoActivo === 'vida') {
        return pol.numPolizaVida || pol.documento || '—';
      }

      if (this.ramoActivo === 'funeraria') {
        return pol.numPolizaFuneraria || pol.documento || '—';
      }

      if (this.ramoActivo === 'accidentes') {
        return pol.numPolizaAccidente || pol.documento || '—';
      }

      if (this.ramoActivo === 'auto') {
        return pol.numPoliza || pol.documento || '—';
      }

      if (this.ramoActivo === 'hogar') {
        return pol.numPolizaHogar || pol.documento || '—';
      }

      return '—';
    },

    companiaActual() {
      return this.nombreCompaniaDesdePoliza(this.polizaActiva);
    },

    vigenciaActual() {
      const pol = this.polizaActiva;
      if (!pol) return '—';
      const desde = pol.vDesde || this.formatDate(pol.polizaDesde) || '—';
      const hasta = pol.vHasta || this.formatDate(pol.polizaHasta) || '—';
      return `${desde} — ${hasta}`;
    },

    pdfUrlActual() {
      const pol = this.polizaActiva;
      if (!pol) return '';
      if (pol.file_url) return pol.file_url;

      if (pol.idDocto) {
        return `https://api-sicas-616002718679.us-central1.run.app/api/api/polizas/pdf/${pol.idDocto}`;
      }

      return '';
    },

    aseguradosDesdePolizasRFC() {
      const rows = [];

      const pushExtraidas = (pol, tipo) => {
        const extra = extraerAseguradosDePolizaData(pol, tipo, pol);
        extra.forEach(r => {
          rows.push({ ...r,
            origen: 'rfc'
          });
        });
      };

      for (const p of this.polizas.gastosMedicos) {
        pushExtraidas(p, 'GMM');
      }

      for (const p of this.polizas.vida) {
        pushExtraidas(p, 'Vida');
      }

      for (const p of this.polizas.funerarias) {
        pushExtraidas(p, 'Funeraria');
      }

      for (const p of this.polizas.accidentes) {
        pushExtraidas(p, 'Accidentes');
      }

      for (const p of this.polizas.auto) {
        pushExtraidas(p, 'Auto');
      }

      for (const p of this.polizas.hogar || []) {
        pushExtraidas(p, 'Hogar');
      }

      return rows;
    },

    aseguradosLista() {
      const desdePolizasRfc = this.aseguradosDesdePolizasRFC;
      const desdeDocs = extraerFilasAseguradosDesdeDocumentos(this.documentosPorEmpresa);
      const merged = fusionarFilasAsegurados(desdePolizasRfc, desdeDocs);
      const deduped = dedupeAseguradosPorPersona(merged);
      const rfcEmp = String(this.empresa.rfc || '').replace(/-/g, '').toUpperCase();

      if (rfcEmp.length === 12) {
        return deduped.filter(r => String(r.rfc).replace(/-/g, '').toUpperCase() !== rfcEmp);
      }

      return deduped;
    },

    indicePolizasPorRfc() {
      const idx = {};

      const add = (rfc, ramo, pol) => {
        if (rfc == null || String(rfc).trim() === '' || String(rfc) === '—') return;
        const key = String(rfc).replace(/\s/g, '').toUpperCase();

        if (!idx[key]) {
          idx[key] = {
            gmm: [],
            vida: [],
            funeraria: [],
            accidentes: [],
            auto: [],
            hogar: []
          };
        }

        idx[key][ramo].push(pol);
      };

      for (const p of this.polizas.gastosMedicos) {
        if (p.rfc) add(p.rfc, 'gmm', p);
        const listaGm = normalizarListaAsegurados(p.dataAsegurados);

        for (const a of listaGm) {
          const r = pickRfcDesdeItem(a);
          if (r) add(r, 'gmm', p);
        }
      }

      for (const p of this.polizas.vida) {
        if (p.rfc) add(p.rfc, 'vida', p);
      }

      for (const p of this.polizas.funerarias) {
        if (p.rfc) add(p.rfc, 'funeraria', p);
      }

      for (const p of this.polizas.accidentes) {
        if (p.rfc) add(p.rfc, 'accidentes', p);
      }

      for (const p of this.polizas.auto) {
        if (p.rfc) add(p.rfc, 'auto', p);
      }

      for (const p of this.polizas.hogar || []) {
        if (p.rfc) add(p.rfc, 'hogar', p);
      }

      for (const item of this.documentosPorEmpresa) {
        const d = item.data;
        const ramo = item.ramo;
        const pol = { ...d,
          _firestoreId: item.id,
          _collection: item.collection,
          _origen: 'nombreEmpresa'
        };
        const filasDoc = extraerAseguradosDePolizaData(d, item.tipo, pol);

        for (const f of filasDoc) {
          if (f.rfc && f.rfc !== '—') add(f.rfc, ramo, pol);
        }
      }

      return idx;
    },

    modalTabsRamo() {
      const k = String(this.modalRfc).replace(/\s/g, '').toUpperCase();
      const bucket = this.indicePolizasPorRfc[k];
      if (!bucket) return [];
      const labels = {
        gmm: 'GMM',
        auto: 'Autos',
        vida: 'Vida',
        funeraria: 'Funeraria',
        accidentes: 'Accidentes',
        hogar: 'Hogar'
      };
      const orden = ['gmm', 'auto', 'vida', 'funeraria', 'accidentes', 'hogar'];
      return orden.filter(key => bucket[key] && bucket[key].length).map(key => ({
        key,
        label: labels[key] || key
      }));
    },

    polizaModalActiva() {
      const k = String(this.modalRfc).replace(/\s/g, '').toUpperCase();
      const bucket = this.indicePolizasPorRfc[k];
      if (!bucket || !this.ramoModalActivo) return null;
      const list = bucket[this.ramoModalActivo];
      return list && list.length ? list[0] : null;
    },

    numPolizaModal() {
      return this.numPolizaPorRamo(this.polizaModalActiva, this.ramoModalActivo);
    },

    companiaModal() {
      return this.nombreCompaniaDesdePoliza(this.polizaModalActiva);
    },

    vigenciaDesdeModal() {
      const pol = this.polizaModalActiva;
      if (!pol) return '—';
      return pol.vDesde != null ? this.formatDate(pol.vDesde) : this.formatDate(pol.polizaDesde) || '—';
    },

    vigenciaHastaModal() {
      const pol = this.polizaModalActiva;
      if (!pol) return '—';
      return pol.vHasta != null ? this.formatDate(pol.vHasta) : this.formatDate(pol.polizaHasta) || '—';
    },

    pdfUrlModal() {
      const pol = this.polizaModalActiva;
      if (!pol) return '';
      if (pol.file_url) return pol.file_url;

      if (pol.idDocto) {
        return `https://api-sicas-616002718679.us-central1.run.app/api/api/polizas/pdf/${pol.idDocto}`;
      }

      return '';
    },

    aseguradosFiltrados() {
      const q = this.busquedaAsegurado.trim().toLowerCase();
      if (!q) return this.aseguradosLista;
      return this.aseguradosLista.filter(r => {
        return r.nombre.toLowerCase().includes(q) || String(r.rfc).toLowerCase().includes(q);
      });
    },

    totalPaginas() {
      return Math.max(1, Math.ceil(this.aseguradosFiltrados.length / PAGE_SIZE));
    },

    aseguradosPagina() {
      const start = (this.paginaActual - 1) * PAGE_SIZE;
      return this.aseguradosFiltrados.slice(start, start + PAGE_SIZE);
    }

  },
  watch: {
    tabsDisponibles: {
      immediate: true,

      handler(list) {
        if (!list.length) return;
        const keys = list.map(t => t.key);

        if (!keys.includes(this.ramoActivo)) {
          this.ramoActivo = list[0].key;
        }

        this.indicePoliza = 0;
      }

    },

    busquedaAsegurado() {
      this.paginaActual = 1;
    },

    aseguradosFiltrados() {
      if (this.paginaActual > this.totalPaginas) {
        this.paginaActual = this.totalPaginas;
      }
    },

    modalDetalle(val) {
      if (false) {}
    }

  },

  mounted() {
    this.cargarPolizas();
  },

  beforeDestroy() {
    if (false) {}
  },

  methods: {
    nombreCompaniaDesdePoliza(pol) {
      if (!pol) return '—';

      if (pol.compania && typeof pol.compania === 'object' && pol.compania.nombreCompania) {
        return pol.compania.nombreCompania;
      }

      const c = pol.compania;

      if (c != null && c !== '' && typeof c === 'string') {
        if (this.catalogoCompanias && this.catalogoCompanias.has(c)) {
          return this.catalogoCompanias.get(c);
        }

        return c;
      }

      return pol.ciaNombre || '—';
    },

    formatDate(val) {
      if (!val) return '';

      if (typeof val.toDate === 'function') {
        const d = val.toDate();
        return Number.isNaN(d.getTime()) ? '' : d.toLocaleDateString('es-MX');
      }

      if (val && typeof val.seconds === 'number') {
        const d = new Date(val.seconds * 1000);
        return d.toLocaleDateString('es-MX');
      }

      if (val instanceof Date) return val.toLocaleDateString('es-MX');
      const str = String(val).trim();

      if (/^\d{4}-\d{2}-\d{2}/.test(str)) {
        const d = new Date(str);
        return Number.isNaN(d.getTime()) ? str : d.toLocaleDateString('es-MX');
      }

      return str;
    },

    seleccionarRamo(key) {
      this.ramoActivo = key;
      this.indicePoliza = 0;
    },

    async cargarPolizas() {
      if (!this.$db || !this.empresa || !this.empresa.rfc) {
        this.errorPolizas = 'No se pudo obtener el RFC de la empresa.';
        this.cargandoPolizas = false;
        return;
      }

      this.errorPolizas = null;
      this.cargandoPolizas = true;

      try {
        const completas = await cargarPolizasPorRfc({
          db: this.$db,
          rfc: this.empresa.rfc
        });
        const {
          polizas,
          ramosPermitidos,
          imgEmpresa,
          imgEmpresaId
        } = await aplicarReglasImgEmpresa(this.$db, this.empresa.nombre, completas);
        this.polizas = polizas;
        this.ramosImgEmpresa = ramosPermitidos;
        this.imgEmpresaRegistro = imgEmpresa ? {
          _docId: imgEmpresaId,
          ...imgEmpresa
        } : null;
        this.sinRegistroImgEmpresa = !imgEmpresa;
        const [docsEmpresa, catalogo] = await Promise.all([fetchTodosDocumentosEmpresa(this.$db, {
          nombre: this.empresa.nombre,
          rfc: this.empresa.rfc,
          imgEmpresaDocId: imgEmpresaId
        }), fetchCatalogoCompaniasMap(this.$db)]);
        this.documentosPorEmpresa = docsEmpresa;
        this.catalogoCompanias = catalogo;
      } catch (e) {
        console.error(e);
        this.errorPolizas = 'No se pudieron cargar las pólizas.';
      } finally {
        this.cargandoPolizas = false;
        this.perfilImgEmpresaResuelto = true;
      }
    },

    tipoARamoKey(tipo) {
      const m = {
        GMM: 'gmm',
        Vida: 'vida',
        Funeraria: 'funeraria',
        Accidentes: 'accidentes',
        Auto: 'auto',
        Hogar: 'hogar'
      };
      return m[tipo] || 'gmm';
    },

    abrirModalAsegurado(row) {
      this.modalNombre = row.nombre;
      this.modalRfc = row.rfc;
      this.modalDetalle = true;
      this.$nextTick(() => {
        const tabs = this.modalTabsRamo;
        if (!tabs.length) return;
        const pref = this.tipoARamoKey(row.tipo);
        const ok = tabs.find(t => t.key === pref);
        this.ramoModalActivo = ok ? pref : tabs[0].key;
      });
    },

    cerrarModalDetalle() {
      this.modalDetalle = false;
    },

    numPolizaPorRamo(pol, ramo) {
      if (!pol) return '—';

      switch (ramo) {
        case 'gmm':
          return pol.numPolizaGM || pol.documento || '—';

        case 'vida':
          return pol.numPolizaVida || pol.documento || '—';

        case 'funeraria':
          return pol.numPolizaFuneraria || pol.documento || '—';

        case 'accidentes':
          return pol.numPolizaAccidente || pol.documento || '—';

        case 'auto':
          return pol.numPoliza || pol.documento || '—';

        case 'hogar':
          return pol.numPolizaHogar || pol.documento || '—';

        default:
          return '—';
      }
    },

    irAPaginaPolizaCompleta() {
      const pol = this.polizaModalActiva;
      if (true) return;
      const ramo = this.ramoModalActivo;

      try {
        if (ramo === 'gmm') {
          sessionStorage.setItem('polizaGMMDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-gmm');
        } else if (ramo === 'vida') {
          sessionStorage.setItem('polizaVidaDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-vida');
        } else if (ramo === 'funeraria') {
          sessionStorage.setItem('polizaFunerariaDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-funeraria');
        } else if (ramo === 'accidentes') {
          sessionStorage.setItem('polizaAccidenteDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-accidente');
        } else if (ramo === 'auto') {
          sessionStorage.setItem('polizaAutoDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-auto');
        } else if (ramo === 'hogar') {
          sessionStorage.setItem('polizaHogarDetalle', JSON.stringify(pol));
          this.$router.push('/poliza-hogar');
        }
      } catch (err) {
        console.error(err);
      }
    },

    etiquetaOpcionPoliza(pol) {
      if (!pol) return '—';

      if (this.ramoActivo === 'gmm') {
        return pol.numPolizaGM || pol.documento || 'GMM';
      }

      if (this.ramoActivo === 'vida') {
        return pol.numPolizaVida || pol.documento || 'Vida';
      }

      if (this.ramoActivo === 'funeraria') {
        return pol.numPolizaFuneraria || pol.documento || 'Funeraria';
      }

      if (this.ramoActivo === 'accidentes') {
        return pol.numPolizaAccidente || pol.documento || 'Accidentes';
      }

      if (this.ramoActivo === 'auto') {
        return pol.descripcionAuto || pol.numPoliza || pol.documento || 'Auto';
      }

      if (this.ramoActivo === 'hogar') {
        return pol.numPolizaHogar || pol.documento || 'Hogar';
      }

      return 'Póliza';
    }

  }
});
// CONCATENATED MODULE: ./components/PortalEmpresaView.vue?vue&type=script&lang=js&
 /* harmony default export */ var components_PortalEmpresaViewvue_type_script_lang_js_ = (PortalEmpresaViewvue_type_script_lang_js_); 
// EXTERNAL MODULE: ./node_modules/vue-loader/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(2);

// CONCATENATED MODULE: ./components/PortalEmpresaView.vue



function injectStyles (context) {
  
  var style0 = __webpack_require__(150)
if (style0.__inject__) style0.__inject__(context)

}

/* normalize component */

var component = Object(componentNormalizer["a" /* default */])(
  components_PortalEmpresaViewvue_type_script_lang_js_,
  render,
  staticRenderFns,
  false,
  injectStyles,
  "7141faa9",
  "4ccaf972"
  
)

/* harmony default export */ var PortalEmpresaView = __webpack_exports__["default"] = (component.exports);

/* nuxt-component-imports */
installComponents(component, {Header: __webpack_require__(61).default})


/***/ }),

/***/ 38:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/Navigation.vue?vue&type=template&id=658ea6e8&scoped=true&
var render = function render() {
  var _vm = this,
      _c = _vm._self._c;

  return _c('ul', _vm._l(_vm.navItems, function (item, index) {
    return _vm._ssrNode("<li data-v-658ea6e8>", "</li>", [_c('n-link', {
      attrs: {
        "to": item.to
      }
    }, [_c('span', [_vm._v(_vm._s(item.label))])])], 1);
  }), 0);
};

var staticRenderFns = [];

// CONCATENATED MODULE: ./components/Navigation.vue?vue&type=template&id=658ea6e8&scoped=true&

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/Navigation.vue?vue&type=script&lang=js&
/* harmony default export */ var Navigationvue_type_script_lang_js_ = ({
  data() {
    return {
      navItems: [{
        to: '/',
        label: 'Inicio'
      }, {
        to: '/about',
        label: 'Nosotros'
      }, {
        to: '/agente',
        label: 'Únete como agente'
      }, {
        to: '/case-studies',
        label: 'Seguros'
      }, {
        to: '/ibsapp',
        label: 'IBSApp'
      }, {
        to: '/siniestros',
        label: 'Siniestros'
      }, {
        to: '/cobranza',
        label: 'Cobranza'
      }, {
        to: '/contact',
        label: 'Contacto'
      }, {
        to: '/descargables',
        label: 'Descargables'
      }]
    };
  }

});
// CONCATENATED MODULE: ./components/Navigation.vue?vue&type=script&lang=js&
 /* harmony default export */ var components_Navigationvue_type_script_lang_js_ = (Navigationvue_type_script_lang_js_); 
// EXTERNAL MODULE: ./node_modules/vue-loader/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(2);

// CONCATENATED MODULE: ./components/Navigation.vue



function injectStyles (context) {
  
  
}

/* normalize component */

var component = Object(componentNormalizer["a" /* default */])(
  components_Navigationvue_type_script_lang_js_,
  render,
  staticRenderFns,
  false,
  injectStyles,
  "658ea6e8",
  "4425b85f"
  
)

/* harmony default export */ var Navigation = __webpack_exports__["default"] = (component.exports);

/***/ }),

/***/ 56:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/HeaderTop.vue?vue&type=template&id=4d1961d3&
var render = function render() {
  var _vm = this,
      _c = _vm._self._c;

  return _c('div', {
    staticClass: "header-top-wrap border-bottom"
  }, [_vm._ssrNode("<div class=\"container-fluid\">", "</div>", [_vm._ssrNode("<div class=\"row\">", "</div>", [_vm._ssrNode("<div class=\"col-lg-12\">", "</div>", [_vm._ssrNode("<p class=\"text-center top-message\">", "</p>", [_c('n-link', {
    attrs: {
      "to": "/"
    }
  }, [_vm._v("Now Hiring:")]), _vm._ssrNode("\n                    Are you a driven and motivated 1st Line IT Support Engineer?\n                ")], 2)])])])]);
};

var staticRenderFns = [];

// CONCATENATED MODULE: ./components/HeaderTop.vue?vue&type=template&id=4d1961d3&

// EXTERNAL MODULE: ./node_modules/vue-loader/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(2);

// CONCATENATED MODULE: ./components/HeaderTop.vue

var script = {}


/* normalize component */

var component = Object(componentNormalizer["a" /* default */])(
  script,
  render,
  staticRenderFns,
  false,
  null,
  null,
  "f20873e6"
  
)

/* harmony default export */ var HeaderTop = __webpack_exports__["default"] = (component.exports);

/***/ }),

/***/ 61:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/vue-loader/lib/loaders/templateLoader.js??ref--6!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/Header.vue?vue&type=template&id=0c34076c&
var render = function render() {
  var _vm = this,
      _c = _vm._self._c;

  return _c('div', {
    staticClass: "header-area header-area--default"
  }, [_c('HeaderTop'), _vm._ssrNode(" "), _c('fixed-header', {
    attrs: {
      "threshold": 300
    }
  }, [_c('div', {
    staticClass: "header-bottom-wrap"
  }, [_c('div', {
    staticClass: "container-fluid"
  }, [_c('div', {
    staticClass: "row"
  }, [_c('div', {
    staticClass: "col-lg-12"
  }, [_c('div', {
    staticClass: "header default-menu-style position-relative"
  }, [_c('div', {
    staticClass: "header__logo"
  }, [_c('n-link', {
    attrs: {
      "to": "/"
    }
  }, [_c('img', {
    staticClass: "img-fluid",
    attrs: {
      "src": "/images/logo/logoIBS.png",
      "alt": "Brand Logo"
    }
  })])], 1), _vm._v(" "), _c('div', {
    staticClass: "header-midle-box"
  }, [_c('div', {
    staticClass: "header-bottom-wrap d-md-block d-none"
  }, [_c('div', {
    staticClass: "header-bottom-inner"
  }, [_c('div', {
    staticClass: "header-bottom-left-wrap"
  }, [_c('div', {
    staticClass: "header__navigation d-none d-xl-block"
  }, [_c('nav', {
    staticClass: "navigation-menu primary--menu"
  }, [_c('Navigation')], 1)])])])])]), _vm._v(" "), _c('div', {
    staticClass: "header-right-box"
  }, [_c('div', {
    staticClass: "header-right-inner",
    attrs: {
      "id": "hidden-icon-wrapper"
    }
  }, [_c('div', {
    staticClass: "header-search-form default-search"
  }, [_c('form', {
    staticClass: "search-form-top"
  }, [_c('input', {
    staticClass: "search-field",
    attrs: {
      "type": "text",
      "placeholder": "Search…"
    }
  }), _vm._v(" "), _c('button', {
    staticClass: "search-submit"
  }, [_c('i', {
    staticClass: "search-btn-icon fa fa-search"
  })])])])]), _vm._v(" "), _c('div', {
    staticClass: "mobile-navigation-icon d-block d-xl-none",
    attrs: {
      "id": "mobile-menu-trigger"
    },
    on: {
      "click": function ($event) {
        return _vm.mobiletoggleClass('addClass', 'active');
      }
    }
  }, [_c('i')])])])])])])])])], 2);
};

var staticRenderFns = [];

// CONCATENATED MODULE: ./components/Header.vue?vue&type=template&id=0c34076c&

// EXTERNAL MODULE: external "vue-fixed-header"
var external_vue_fixed_header_ = __webpack_require__(31);
var external_vue_fixed_header_default = /*#__PURE__*/__webpack_require__.n(external_vue_fixed_header_);

// EXTERNAL MODULE: ./components/HeaderTop.vue + 2 modules
var HeaderTop = __webpack_require__(56);

// EXTERNAL MODULE: ./components/Navigation.vue + 4 modules
var Navigation = __webpack_require__(38);

// CONCATENATED MODULE: ./node_modules/babel-loader/lib??ref--2-0!./node_modules/@nuxt/components/dist/loader.js??ref--0-0!./node_modules/vue-loader/lib??vue-loader-options!./components/Header.vue?vue&type=script&lang=js&



/* harmony default export */ var Headervue_type_script_lang_js_ = ({
  components: {
    HeaderTop: HeaderTop["default"],
    Navigation: Navigation["default"],
    FixedHeader: external_vue_fixed_header_default.a
  },
  methods: {
    // offcanvas mobilemenu open
    mobiletoggleClass(addRemoveClass, className) {
      const el = document.querySelector('#offcanvas-menu');

      if (addRemoveClass === 'addClass') {
        el.classList.add(className);
      } else {
        el.classList.remove(className);
      }
    }

  }
});
// CONCATENATED MODULE: ./components/Header.vue?vue&type=script&lang=js&
 /* harmony default export */ var components_Headervue_type_script_lang_js_ = (Headervue_type_script_lang_js_); 
// EXTERNAL MODULE: ./node_modules/vue-loader/lib/runtime/componentNormalizer.js
var componentNormalizer = __webpack_require__(2);

// CONCATENATED MODULE: ./components/Header.vue





/* normalize component */

var component = Object(componentNormalizer["a" /* default */])(
  components_Headervue_type_script_lang_js_,
  render,
  staticRenderFns,
  false,
  null,
  null,
  "cf530450"
  
)

/* harmony default export */ var Header = __webpack_exports__["default"] = (component.exports);

/* nuxt-component-imports */
installComponents(component, {HeaderTop: __webpack_require__(56).default,Navigation: __webpack_require__(38).default})


/***/ })

};;
//# sourceMappingURL=portal-empresa-view.js.map